---
steps:
  - command: 'make geth && chmod -R a+rwx $GOPATH'
    label: "build geth"
    env:
      BUILDKITE_GOLANG_IMPORT_PATH: "github.com/ledgerwatch/turbo-geth"

  - command: './nightly.sh'
    label: 'run geth for a bit'
