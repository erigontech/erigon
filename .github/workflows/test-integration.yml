name: Integration tests 123
on:
  push:
    branches:
      - devel
      - alpha
      - stable
      - ciint1
  schedule:
    - cron: '20 16 * * *' # daily at 16:20 UTC
jobs:
  tests:
    runs-on: ubuntu-20.04

    steps:
      - run: echo ok 123
