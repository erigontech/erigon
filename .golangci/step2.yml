run:
  deadline: 10m

linters:
  enable:
    - depguard
    - deadcode
    - govet
    - structcheck
    - stylecheck
    - typecheck

  disable-all: true

linters-settings:
    govet:
      check-shadowing: true